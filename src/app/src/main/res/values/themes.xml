<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme sin ActionBar para todas las actividades -->
    <style name="Base.Theme.UNIGO" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.UNIGO" parent="Base.Theme.UNIGO" />

    <!-- Tema especial para SplashScreen con pantalla completa -->
    <style name="Theme.UNIGO.Fullscreen" parent="Theme.UNIGO">
        <item name="android:windowFullscreen">true</item>
    </style>
</resources>